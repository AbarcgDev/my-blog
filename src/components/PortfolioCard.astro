---
import { Icon } from "astro-icon/components";
import { getLangIcon, getTechIcon } from "../utils/techIcons";

const { project } = Astro.props;
---

<article class="project-card">
    {/* ── Status bar ── */}
    <div class="status-bar">
        <div class="status-left">
            <span class={`health-dot ${project.health}`}></span>
            <span class={`health-label ${project.health}`}>
                {project.healthLabel}
            </span>
        </div>
        <span class="uptime-val">
            uptime <strong>{project.uptime}</strong>
        </span>
    </div>

    {/* ── Title ── */}
    <div class="card-title-row">
        <span class="lang-glyph">
            <Icon name={getLangIcon(project.data.language || "default")} />
        </span>
        <div>
            <h2 class="card-name">
                {project.data.title}
            </h2>
            <p class="card-desc">
                {project.data.description}
            </p>
            {/* ── Stack tags ── */}
            {
                project.data.tags.length > 0 && (
                    <div class="stack-row">
                        {project.data.tags.map((tag: string) => (
                            <span class="tech-tag" title={tag}>
                                <Icon name={getTechIcon(tag)} />
                            </span>
                        ))}
                    </div>
                )
            }
        </div>
    </div>

    {/* ── Metrics grid ── */}
    <div class="metrics-grid">
        <div class="metric-box" title="Stars">
            <span class="mb-val">{project.data.stars}</span>
            <span class="mb-label">
                <Icon name="mdi:star" />
            </span>
        </div>
        <div class="metric-box" title="Edad">
            <span class="mb-val">{project.ageMonths}m</span>
            <span class="mb-label">
                <Icon name="mdi:calendar" />
            </span>
        </div>
        <div class="metric-box" title="Último push">
            <span class="mb-val">{project.daysSinceUpdate}d</span>
            <span class="mb-label">
                <Icon name="mdi:source-commit" />
            </span>
        </div>
        <div class="metric-box" title="Dependencias">
            <span class="mb-val">{project.data.tags.length}</span>
            <span class="mb-label">
                <Icon name="mdi:package-variant" />
            </span>
        </div>
    </div>

    {/* ── Footer ── */}
    <div class="card-footer">
        <a
            href={project.data.repoUrl}
            target="_blank"
            rel="noopener"
            class="repo-link"
            title="Repository"
        >
            <Icon name="mdi:github" />
        </a>
        <a
            href={`/portfolio/${project.id}/`}
            class="detail-link"
            title="Ver detalle"
        >
            <Icon name="mdi:arrow-right" />
        </a>
    </div>
</article>

<style>
    /* ═══ CARD ═══ */
    .project-card {
        background: var(--surface);
        border: 1px solid var(--gray-light);
        border-radius: 14px;
        overflow: hidden;
        box-shadow: var(--box-shadow);
        transition: border-color 0.3s ease;
    }

    .project-card:hover {
        border-color: var(--primary);
    }

    /* ── Status bar ── */
    .status-bar {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 0.5rem 1.2rem;
        background: color-mix(in srgb, var(--gray-light) 30%, transparent);
        border-bottom: 1px solid var(--gray-light);
    }

    .status-left {
        display: flex;
        align-items: center;
        gap: 0.4rem;
    }

    .health-dot {
        width: 8px;
        height: 8px;
        border-radius: 50%;
    }

    .health-dot.green {
        background: #22c55e;
        box-shadow: 0 0 4px rgba(34, 197, 94, 0.5);
    }
    .health-dot.yellow {
        background: #eab308;
        box-shadow: 0 0 4px rgba(234, 179, 8, 0.5);
    }
    .health-dot.red {
        background: #ef4444;
        box-shadow: 0 0 4px rgba(239, 68, 68, 0.5);
    }

    .health-label {
        font-family: "Fira Code", monospace;
        font-size: 0.6rem;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.06em;
    }

    .health-label.green {
        color: #22c55e;
    }
    .health-label.yellow {
        color: #eab308;
    }
    .health-label.red {
        color: #ef4444;
    }

    .uptime-val {
        font-family: "Fira Code", monospace;
        font-size: 0.6rem;
        color: var(--gray);
    }

    .uptime-val strong {
        color: var(--primary);
    }

    /* ── Title ── */
    .card-title-row {
        display: flex;
        align-items: flex-start;
        gap: 0.8rem;
        padding: 1rem 1.2rem 0;
    }

    .lang-glyph {
        font-size: 2.2rem;
        color: var(--primary);
        flex-shrink: 0;
        margin-top: 0.1rem;
    }

    .card-name {
        font-family: "Fira Code", monospace;
        font-size: 1.05rem;
        font-weight: 700;
        color: var(--black);
        margin: 0;
        line-height: 1.3;
    }

    .card-desc {
        font-size: 0.78rem;
        color: var(--gray);
        margin: 0.2rem 0 0 0;
        line-height: 1.4;
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }

    /* ── Metrics grid ── */
    .metrics-grid {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 0.5rem;
        padding: 0.8rem 1.2rem;
    }

    .metric-box {
        text-align: center;
        padding: 0.4rem;
        border-radius: 8px;
        background: color-mix(in srgb, var(--primary) 5%, transparent);
        border: 1px solid color-mix(in srgb, var(--primary) 10%, transparent);
    }

    .mb-val {
        font-family: "Fira Code", monospace;
        font-size: 1rem;
        font-weight: 800;
        color: var(--primary);
        display: block;
        line-height: 1.2;
    }

    .mb-label {
        font-family: "Fira Code", monospace;
        font-size: 0;
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .mb-label [data-icon] {
        font-size: 1.8rem;
        color: var(--primary);
    }

    /* ── Stack ── */
    .stack-row {
        display: flex;
        gap: 0.6rem;
        flex-wrap: wrap;
        margin-top: 0.8rem;
    }

    .tech-tag {
        font-size: 0;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        transition: transform 0.2s ease;
    }

    .tech-tag:hover {
        transform: translateY(-2px);
    }

    .tech-tag [data-icon] {
        font-size: 1.5rem;
    }

    /* ── Footer ── */
    .card-footer {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 0.6rem 1.2rem;
        border-top: 1px solid var(--gray-light);
    }

    .repo-link,
    .detail-link {
        font-family: "Fira Code", monospace;
        font-size: 0;
        text-decoration: none;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        padding: 0.4rem;
        transition: color 0.2s ease, transform 0.2s ease;
    }
    .repo-link [data-icon],
    .detail-link [data-icon] {
        font-size: 2rem;
    }

    .repo-link {
        color: var(--gray);
    }
    .repo-link:hover {
        color: var(--primary);
    }
    .detail-link {
        color: var(--primary);
    }
    .detail-link:hover {
        color: var(--accent);
    }

    @media (max-width: 640px) {
        .metrics-grid {
            grid-template-columns: repeat(2, 1fr);
        }
    }
</style>
